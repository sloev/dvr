ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=NO

# Add your home/studio network here before first boot.
# The app manages additional networks at runtime.
network={
    ssid="YourNetworkSSID"
    psk="YourPassword"
    priority=10
}
