[package]
name = "dvr_app"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { version = "1.5.0", features = ["linuxkms"] }
gstreamer = "0.22.0"
gstreamer-video = "0.22.0"
gstreamer-app = "0.22.0"

[build-dependencies]
slint-build = "1.5.0"
